const TicketArchive = require('../model/TicketArchive');
const Project = require('../../project/model/Project');

const archiveTicket = async (req, res, next) => {
  try {
  } catch (e) {
    next(e);
  }
};

const unArchiveTicket = async (req, res, next) => {
  try {
  } catch (e) {
    next(e);
  }
};

const getAllArchivedTickets = async (req, res, next) => {
  try {
  } catch (e) {
    next(e);
  }
};

module.exports = {
  archiveTicket,
  unArchiveTicket,
  getAllArchivedTickets,
};
