const ProjectArchive = require('../model/ProjectArchive');
const Project = require('../../project/model/Project');

const archiveProject = async (req, res, next) => {
  try {
  } catch (e) {
    next(e);
  }
};

const unArchiveProject = async (req, res, next) => {
  try {
  } catch (e) {
    next(e);
  }
};

const getAllArchivedProjects = async (req, res, next) => {
  try {
  } catch (e) {
    next(e);
  }
};

module.exports = {
  archiveProject,
  unArchiveProject,
  getAllArchivedProjects,
};
